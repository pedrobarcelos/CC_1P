#include <stdio.h>
#include <stdlib.h>

	int main (){
        int X;
        int rest;
        int quo;

        printf("\nDescobrir se um valor é par\n");
        printf("\nDigite um valor: \n")
        scanf("%i",&X);
        rest = X % 2;
        if (rest != 0)
        {
            printf("\nO numeoro inserido é impar");
        }
        (rest != 0)
            printf("\nO numeoro inserido é impar");
        else
        {
            printf("\nO numero inserido é par");
        }
    }
        
        
        